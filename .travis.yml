language: rust
rust:
  - stable
  - beta
  - nightly
  - 1.14.0

script:
  - cargo build --verbose
  - cargo test --verbose
  - if [ "$(rustup show | grep default | grep stable)" != "" ]; then cd fuzz && cargo test --verbose && ./travis-fuzz.sh; fi
